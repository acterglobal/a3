import 'package:flutter/material.dart';

class SubSpaces extends StatelessWidget {
  final String spaceId;

  const SubSpaces({super.key, required this.spaceId});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
